# PyTorch CUDA wheel (change cu124 to match your installed CUDA version, e.g. cu118 or cu121).
# CPU-only users: replace --index-url with --extra-index-url and remove the --extra-index-url pypi line.
#
# NOTE: irodori-tts is NOT listed here — the upstream repo has two packaging
# bugs (ambiguous flat-layout + invalid license field) that require a manual
# clone-and-patch. After installing this file, run the appropriate script:
#   Windows:     install-irodori.ps1
#   Linux/macOS: bash install-irodori.sh
--index-url https://download.pytorch.org/whl/cu124
--extra-index-url https://pypi.org/simple
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.5.0
torch>=2.5.0
torchaudio>=2.5.0
huggingface_hub>=0.23.0
