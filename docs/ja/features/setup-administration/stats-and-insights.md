---
title: "統計とインサイト"
sidebar:
  order: 4
---

TomoriBotは利用状況をトラッキングし、誰が誰と話しているか、どのペルソナとモデルが使用されているか、どのツールが実行されたかを確認できるようにします。そして、それを共有可能なインフォグラフィックに変換します。

## テキストダッシュボード

3つのコマンドを使用すると、タブで切り替え可能なインタラクティブなダッシュボード（概要、ペルソナ、モデルとコスト、ツールとコマンド、表現、お気に入りの人、リーダーボード）が開きます。

テキストタブは、呼び出し元が制御する永続的な公開ダッシュボードです。メッセージが削除されるまで利用可能であり、他のユーザーが操作することはできません。

- `/stats personal`：あなた自身の利用状況。
- `/stats persona`：このサーバーでのペルソナの利用状況。
- `/stats server`：サーバー全体の利用状況。

ほとんどの場合、**期間**（timeframe）を指定でき、個人の統計はこのサーバーのみに絞り込むことも、すべてのサーバーを対象にすることもできます。

:::note
**トークン数**は、利用可能な場合はプロバイダー自体が報告した使用量です（報告しないプロバイダーにのみ文字ベースの推定値が使用されます）。**コスト**は、モデルカタログの定価に基づいて計算されるため、実際の請求額（プロンプトキャッシング、割引、無料枠のクオータなど）とは異なる場合があります。
:::

## 共有可能なインフォグラフィックカード

`/stats generate`は、チャットにドロップできる洗練された画像カードをレンダリングします。

- **パーソナルWrapped**：あなた個人のアクティビティを、Spotifyまとめ（Spotify Wrapped）のようなスタイルで表示します。
- **ペルソナの親和性**：このサーバーでのペルソナの統計です。
- **サーバーリーダーボード**：サーバー全体のランキングです。

完全にプライベートなユーザー（`/personal config`）は個人のカードを生成できません。

カードの構成とレンダリングの仕組みについては、[統計インフォグラフィックサブシステム](/en/architecture/subsystems/stats-infographic/)のアーキテクチャリファレンスをご覧ください。
