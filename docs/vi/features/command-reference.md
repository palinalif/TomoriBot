---
title: "Danh mục lệnh"
sidebar:
  order: 6
---

<!--
  GENERATED FILE: do not edit by hand.
  Run `bun run generate-command-reference` from the repository root.
-->

Tất cả các lệnh slash hiện được TomoriBot đăng ký, được tạo từ cùng trình xây dựng lệnh và mô tả ngôn ngữ tiếng Anh dùng để đăng ký trên Discord.

Các nhóm lệnh cấp cao nhất: **39**. Các lệnh slash có thể thực thi: **81**.

## `/comment`

Gửi một embed bình luận hiển thị trong đoạn chat nhưng ẩn khỏi ngữ cảnh.

| Lệnh | Tóm tắt |
|---|---|
| `/comment` | Gửi một embed bình luận hiển thị trong đoạn chat nhưng ẩn khỏi ngữ cảnh. |

## `/compact`

Tóm tắt cuộc trò chuyện gần đây thành một bộ nhớ hệ thống cô đọng.

| Lệnh | Tóm tắt |
|---|---|
| `/compact` | Tóm tắt cuộc trò chuyện gần đây thành một bộ nhớ hệ thống cô đọng. |

## `/conditioning`

Quản lý bộ nhớ điều kiện khen thưởng và trừng phạt liên tục.

| Lệnh | Tóm tắt |
|---|---|
| `/conditioning manage` | Quản lý lịch sử điều kiện hóa đã chèn trên tất cả các persona trong máy chủ này. |
| `/conditioning remove` | Xóa các mục điều kiện hóa trên mọi persona trong máy chủ này. |

## `/config`

Cấu hình cài đặt persona, hành vi, kênh, quyền hạn và model.

| Lệnh | Tóm tắt |
|---|---|
| `/config` | Cấu hình cài đặt persona, hành vi, kênh, quyền hạn và model. |

## `/contribute`

Tìm mã nguồn và các cách hỗ trợ xây dựng TomoriBot.

| Lệnh | Tóm tắt |
|---|---|
| `/contribute github` | Lấy liên kết kho lưu trữ GitHub và tìm hiểu cách đóng góp cho TomoriBot. |

## `/donate`

Ủng hộ chi phí phát triển và lưu trữ của TomoriBot.

| Lệnh | Tóm tắt |
|---|---|
| `/donate kofi` | Hỗ trợ phát triển TomoriBot thông qua đóng góp trên Ko-fi. |

## `/export`

Xuất cấu hình hoặc bộ nhớ của bạn thành một tệp có thể chuyển giao.

| Lệnh | Tóm tắt |
|---|---|
| `/export config` | Xuất cấu hình máy chủ này thành một tệp có thể chuyển giao. |
| `/export memories` | Xuất bộ nhớ thành một tệp có thể chuyển giao. |
| `/export personal config` | Xuất cấu hình cá nhân của bạn thành một tệp có thể chuyển giao. |
| `/export personal memories` | Xuất các bộ nhớ thuộc sở hữu tài khoản của bạn thành một tệp có thể chuyển giao. |

## `/expressions`

Dạy TomoriBot thời điểm sử dụng emoji tùy chỉnh và sticker của máy chủ này.

| Lệnh | Tóm tắt |
|---|---|
| `/expressions edit` | Chỉnh sửa cảm xúc và hướng dẫn sử dụng của một emoji hoặc sticker đơn lẻ |
| `/expressions initialize` | Phân tích và phân loại tất cả emoji tùy chỉnh và sticker bằng AI thị giác |

## `/generate`

Tạo hình ảnh AI, video và tin nhắn thoại.

| Lệnh | Tóm tắt |
|---|---|
| `/generate image` | Tạo hình ảnh AI từ prompt của riêng bạn hoặc từ ngữ cảnh kênh hiện tại |
| `/generate scene` | Tạo một phân cảnh văn bản ngắn có kịch bản giữa các persona đã chọn. |
| `/generate video` | Tạo video AI bằng Google Veo, OpenRouter hoặc Z.ai |
| `/generate voice-message` | Đọc một tin nhắn bằng giọng nói do bạn chọn |

## `/help`

Duyệt hướng dẫn cài đặt, tính năng, nhà cung cấp, bộ nhớ, hành vi, công cụ, phương tiện và tích hợp.

| Lệnh | Tóm tắt |
|---|---|
| `/help` | Duyệt hướng dẫn cài đặt, tính năng, nhà cung cấp, bộ nhớ, hành vi, công cụ, phương tiện và tích hợp. |

## `/impersonate`

Hóa thân thành persona, người dùng hoặc chèn prompt hệ thống.

| Lệnh | Tóm tắt |
|---|---|
| `/impersonate persona` | Gửi tin nhắn dưới tư cách một trong các persona của máy chủ này. |
| `/impersonate system` | Chèn một tin nhắn hệ thống vào ngữ cảnh cuộc trò chuyện. |
| `/impersonate user` | Yêu cầu bot viết và gửi tin nhắn như thể đó là thành viên đó. |

## `/import`

Nhập cấu hình hoặc bộ nhớ từ một tệp có thể chuyển giao.

| Lệnh | Tóm tắt |
|---|---|
| `/import config` | Nhập tệp cấu hình máy chủ. |
| `/import memories` | Nhập tệp bộ nhớ máy chủ. |
| `/import personal config` | Nhập tệp cấu hình cá nhân. |
| `/import personal memories` | Nhập tệp bộ nhớ cá nhân. |

## `/kill`

Dừng ngay luồng phát hiện tại và xóa các phản hồi đang xếp hàng trong kênh này.

| Lệnh | Tóm tắt |
|---|---|
| `/kill` | Dừng ngay luồng phát hiện tại và xóa các phản hồi đang xếp hàng trong kênh này. |

## `/learn`

Học hỏi, trích xuất và nạp lịch sử trò chuyện vào bộ nhớ.

| Lệnh | Tóm tắt |
|---|---|
| `/learn history` | Trích xuất tri thức từ lịch sử tin nhắn của kênh này bằng AI. |

## `/legal`

Xem điều khoản dịch vụ, chính sách quyền riêng tư và giấy phép của TomoriBot.

| Lệnh | Tóm tắt |
|---|---|
| `/legal license` | Xem giấy phép mã nguồn mở của TomoriBot |
| `/legal privacy-policy` | Xem Chính sách quyền riêng tư của TomoriBot |
| `/legal terms-of-service` | Xem Điều khoản dịch vụ của TomoriBot |

## `/matrix`

Liên kết các kênh Discord với các phòng Matrix để chuyển tiếp hai chiều.

| Lệnh | Tóm tắt |
|---|---|
| `/matrix link` | Liên kết một kênh Discord với một phòng Matrix để chuyển tiếp hai chiều |
| `/matrix unlink` | Xóa liên kết Matrix bridge khỏi một kênh Discord |

## `/memories`

Kiểm tra và quản lý bộ nhớ máy chủ, tài liệu và bộ nhớ ngắn hạn.

| Lệnh | Tóm tắt |
|---|---|
| `/memories` | Kiểm tra và quản lý bộ nhớ máy chủ, tài liệu và bộ nhớ ngắn hạn. |

## `/model`

Quản lý các model AI mặc định của máy chủ này.

| Lệnh | Tóm tắt |
|---|---|
| `/model override remove` | Xóa các tùy chỉnh model theo kênh và theo persona. |

## `/moderation`

Quản lý quyền thành viên, danh sách đen, danh sách trắng kênh và vai trò, cùng các cài đặt hạn ngạch.

| Lệnh | Tóm tắt |
|---|---|
| `/moderation` | Quản lý quyền thành viên, danh sách đen, danh sách trắng kênh và vai trò, cùng các cài đặt hạn ngạch. |

## `/novelai`

Cấu hình tạo văn bản và hình ảnh NovelAI cho máy chủ này.

| Lệnh | Tóm tắt |
|---|---|
| `/novelai generate image` | Tạo hình ảnh NovelAI bằng các thẻ tag phong cách imageboard và ảnh nhân vật tham chiếu tùy chọn. |
| `/novelai usage` | Hiển thị đồng hồ đo mức sử dụng NovelAI Opus của máy chủ này (yêu cầu quyền Manage Server). |

## `/nsfw`

Các lệnh và cài đặt giới hạn độ tuổi.

| Lệnh | Tóm tắt |
|---|---|
| `/nsfw jailbreaks` | Quản lý các hành vi jailbreak tùy chọn cho prompt của mình trên máy chủ này. |

## `/nuke`

Xóa sạch toàn bộ dữ liệu máy chủ. Yêu cầu chạy lại /setup sau đó.

| Lệnh | Tóm tắt |
|---|---|
| `/nuke` | Xóa sạch toàn bộ dữ liệu máy chủ. Yêu cầu chạy lại /setup sau đó. |

## `/persona`

Quản lý các preset cá tính

| Lệnh | Tóm tắt |
|---|---|
| `/persona create` | Tự tạo một preset cá tính đơn giản theo cách thủ công |
| `/persona default` | Áp dụng một cấu hình cá tính tạo sẵn |
| `/persona export` | Xuất cá tính hiện tại thành tệp PNG có thể chia sẻ |
| `/persona generate` | Tạo cá tính bằng AI (yêu cầu một nhà cung cấp tương thích) |
| `/persona import` | Nhập một persona từ tệp PNG, JSON hoặc CHARX |
| `/persona remove` | Xóa một persona alter khỏi máy chủ |

## `/personal`

Quản lý các cài đặt cá nhân của bạn

| Lệnh | Tóm tắt |
|---|---|
| `/personal config` | Quản lý các tùy chọn cá nhân, quyền riêng tư, model và hồ sơ của bạn. |
| `/personal language` | Chọn ngôn ngữ TomoriBot dùng để nói chuyện với bạn. |
| `/personal memories` | Quản lý bộ nhớ dài hạn cá nhân và ngữ cảnh trò chuyện ngắn hạn của bạn. |
| `/personal nuke` | Xóa mọi thứ TomoriBot lưu trữ về bạn, trên mọi máy chủ. |
| `/personal providers` | Quản lý thông tin xác thực nhà cung cấp, endpoint và danh mục model cá nhân của bạn. |

## `/ping`

Kiểm tra độ trễ của bot.

| Lệnh | Tóm tắt |
|---|---|
| `/ping` | Kiểm tra độ trễ của bot. |

## `/providers`

Thêm, xem, chỉnh sửa và xóa thông tin xác thực nhà cung cấp, endpoint và danh mục model.

| Lệnh | Tóm tắt |
|---|---|
| `/providers` | Thêm, xem, chỉnh sửa và xóa thông tin xác thực nhà cung cấp, endpoint và danh mục model. |

## `/punish`

Phạt mình bằng các tương tác vui vẻ.

| Lệnh | Tóm tắt |
|---|---|
| `/punish bite` | Cắn yêu mình một cái! |
| `/punish bonk` | Gõ nhẹ vào đầu mình một cái! |
| `/punish pinch` | Véo mình một cái! |
| `/punish spank` | Đét yêu mình một cái! |
| `/punish squeeze` | Bóp nhẹ mình một cái! |

## `/quota`

Quản lý đặt lại hạn ngạch tạo nội dung.

| Lệnh | Tóm tắt |
|---|---|
| `/quota reset global` | Đặt lại nhóm hạn ngạch tạo nội dung trên toàn máy chủ. |
| `/quota reset user` | Đặt lại mức sử dụng hạn ngạch hàng ngày cho một người dùng. |

## `/refresh`

Xóa lịch sử trò chuyện (chỉ trong kênh này).

| Lệnh | Tóm tắt |
|---|---|
| `/refresh` | Xóa lịch sử trò chuyện (chỉ trong kênh này). |

## `/reset`

Đặt lại cấu hình máy chủ hoặc cá nhân về mặc định.

| Lệnh | Tóm tắt |
|---|---|
| `/reset config` | Đặt lại cấu hình của máy chủ này về mặc định cơ sở dữ liệu. |
| `/reset personal config` | Đặt lại cấu hình cá nhân của bạn về mặc định cơ sở dữ liệu. |

## `/respond`

Kích hoạt thủ công phản hồi cho tin nhắn mới nhất trong kênh này.

| Lệnh | Tóm tắt |
|---|---|
| `/respond` | Kích hoạt thủ công phản hồi cho tin nhắn mới nhất trong kênh này. |

## `/reward`

Thưởng cho mình bằng các tương tác thú vị.

| Lệnh | Tóm tắt |
|---|---|
| `/reward feed` | Cho mình ăn một món ăn vặt ngon lành! |
| `/reward headpat` | Xoa đầu mình một cái! |
| `/reward hug` | Ôm mình một cái! |
| `/reward kiss` | Hôn mình một cái! |
| `/reward tickle` | Cù lét mình! |

## `/scheduled-task`

Quản lý các tác vụ theo lịch và lời nhắc.

| Lệnh | Tóm tắt |
|---|---|
| `/scheduled-task edit` | Chỉnh sửa một tác vụ theo lịch hoặc lời nhắc. |
| `/scheduled-task remove` | Xóa một tác vụ theo lịch hoặc lời nhắc. |

## `/setup`

Bắt đầu quy trình thiết lập ban đầu. Cấu hình nhà cung cấp AI và cá tính.

| Lệnh | Tóm tắt |
|---|---|
| `/setup` | Bắt đầu quy trình thiết lập ban đầu. Cấu hình nhà cung cấp AI và cá tính. |

## `/stats`

Xem số liệu thống kê sử dụng

| Lệnh | Tóm tắt |
|---|---|
| `/stats generate` | Tạo thẻ ảnh thống kê có thể chia sẻ. |
| `/stats persona` | Xem số liệu thống kê sử dụng của một persona trên máy chủ này. |
| `/stats personal` | Xem số liệu thống kê sử dụng của chính bạn. |
| `/stats server` | Xem số liệu thống kê sử dụng trên toàn máy chủ. |

## `/status`

Hiển thị trạng thái hiện tại của cá nhân, máy chủ hoặc persona.

| Lệnh | Tóm tắt |
|---|---|
| `/status` | Hiển thị trạng thái hiện tại của cá nhân, máy chủ hoặc persona. |

## `/support`

Nhận trợ giúp, báo lỗi và tham gia cộng đồng TomoriBot.

| Lệnh | Tóm tắt |
|---|---|
| `/support discord` | Lấy liên kết máy chủ Discord chính thức để báo lỗi, gửi phản hồi và trò chuyện cùng cộng đồng. |

## `/tool`

Các thao tác tiện ích cho ngữ cảnh trò chuyện, prompt và chẩn đoán.

| Lệnh | Tóm tắt |
|---|---|
| `/tool delete turn` | Xóa lượt phản hồi cuối cùng của persona khỏi kênh. |
| `/tool estimate cost` | Ước tính chi phí API cho các nhà cung cấp AI trả phí |
| `/tool prompt snapshot` | Xuất chính xác prompt LLM của một persona ra tệp để gỡ lỗi. |

## `/update`

Xem ghi chú phát hành mới nhất của TomoriBot

| Lệnh | Tóm tắt |
|---|---|
| `/update` | Xem ghi chú phát hành mới nhất của TomoriBot |
